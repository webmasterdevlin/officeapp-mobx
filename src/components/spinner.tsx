import * as React from "react";
//@ts-ignore
import Loader from "react-loader-spinner";

const Spinner = () => (
  <Loader type="Oval" color="dodgerblue" height="80" width="80" />
);

export default Spinner;
